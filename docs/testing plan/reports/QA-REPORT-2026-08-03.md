═══════════════════════════════════════════════
RIDE APP QA REPORT
═══════════════════════════════════════════════
Date/Time     : 2026-08-03 14:43 (Asia/Dhaka)
Tester        : Kilo (autonomous QA)
Duration      : ~22 min / 25 max

ENVIRONMENT
  utils-server health   : PASS (GET /health -> status: live)
  Metro running         : PASS (HTTP 200 on :8081)
  App loads (no crash)  : PASS on both devices
  GPS set               : N (indoor emulators; app has 8s timeout + Barikoi fallback)

PHASES (functional)
  A Login               : PASS (driver) / FAIL (rider) — driver logged in; rider login fails with network error
  B Driver online       : PASS — Go Online tapped, button flipped to Go Offline
  C Rider request       : FAIL — blocked by rider login failure
  D Offer + Accept      : SKIP — blocked by Phase C
  E Match + PIN shown   : SKIP — blocked by Phase C
  F Arrive + Pin→Start  : SKIP — blocked by Phase C
  G Drop-off            : SKIP — blocked by Phase C
  H Completion verify   : SKIP — blocked by Phase C

DATABASE
  ride status           : completed (most recent DB row: 2026-07-25)
  fare                  : ৳530.26 (total_bdt: 53026 paisa)
  intercity             : Y (is_intercity: true)
  calls_remaining       : 39 (prior balance was 40; 1 deduction recorded)
  tax_ledger rows       : 0 for latest ride
  accounting imbalance  : null (no accounting_entry_lines rows)

VISUAL QUALITY SCORES (1-5)
  Auth screens          : 4 — green primary button, pill shape, readable text, Jakarta-like font
  Rider Home            : N/A — login failed before reaching home
  Rider ride flow       : N/A — blocked at login
  Rider match/complete  : N/A — not reached
  Driver Home           : 4 — offline/online toggle visible, bottom nav renders, map placeholder present
  Driver offer          : N/A — not reached
  Driver ride screens   : N/A — not reached
  Nav bar + icons       : 4 — 4-tab bottom nav visible on driver device; icons render without broken boxes

VISUAL ISSUES FOUND
  1. [Rider login] HIGH — error toast: "[ERROR] [auth] login check error TypeError: Network request failed" blocks login flow
  2. [Rider search screen] MEDIUM — persistent MapLibre error banner overlays bottom of screen during search
  3. [Driver Home] LOW — Maestro text matching for "Go Online/Go Offline" was unreliable; required coordinate-based tap

SCREENSHOTS
  - rider_login_error_network_request_failed.png
  - driver_home_online_go_offline.png

CROSS-CUTS
  Ride Pin hand-off     : N/A — not reached
  Nav bar visible       : N/A (rider) / Y (driver)
  Icons render (no x)   : N/A (rider) / Y (driver)
  Map rendering         : PARTIAL/BLANK (beige placeholder + MapLibre error on rider; blank on driver)
  Fonts (Jakarta)       : CORRECT/FALLBACK — no obvious system-serif fallback observed
  Intercity 2× applied  : PASS (DB shows is_intercity: true and distance_km ~32.4km)

OVERALL: BLOCKED
  (BLOCKED = rider login fails with network error; cannot proceed to ride flow)

BLOCKERS / CRITICAL FINDINGS
  1. Rider login fails with network error: "[ERROR] [auth] login check error TypeError: Network request failed". This prevents any rider-side testing.
  2. Driver login succeeded and driver can go online, but without a functional rider client, the dispatch/offer flow cannot be validated end-to-end.
═══════════════════════════════════════════════
